//22/08/2022//

//EVALUACION

/*Escriba una funcion que llene un array con n numeros (debe ser minimo 5 y maximo 25)*/

function llenararray() {
    const n = []
    tam = Math.round((Math.random() * 10 - 5) + 15)
    for (let i = 0; i < tam; i++) {
        n[i] = Math.round(Math.random() * 100);

    }
    console.log(n)
}
llenararray();

/*Escriba una funcion que reciba el array lleno y retorne otro arreglo con la diferencia que
 hay entre el valor de cada elemento y la media del arreglo.*/

apply(llenararray)
return new array = (nuevoarray)

function nuevoarray() {

    for (let i = 0; i < tam; i++) {
        n[i] = Math.round(Math.random() * 100);
    }

}

function array(array2) {

}





/*Escriba una funcion que reciba ele arreglo con las diferencias del punto anterior y 
diga de cada uno cuanto le falta o cuanto le sobra segun el caso con referencia a la media.*/